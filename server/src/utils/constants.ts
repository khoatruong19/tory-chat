export enum Routes {
  AUTH = 'auth',
  USERS = 'users',
  FRIENDSHIPS = 'friends',
  MESSAGES = 'conversations/:id/messages',
}

export enum Services {
  AUTH = 'AUTH_SERVICE',
  USERS = 'USERS_SERVICE',
  FRIENDSHIPS = 'FRIENDSHIPS_SERVICE',
  MESSAGES = 'MESSAGES_SERVICE',
  GATEWAY_SESSION_MANAGER = 'GATEWAY_SESSION_MANAGER',
}
